export enum SizeCategory {
    Small,
    Medium,
    Large,
    XL
}

export enum SizeDimensions {
    small = 10,
    medium = 50,
    large = 100,
    XL = 100 // Adding for later checks
}

export enum SizePrice {
    small = 3,
    medium = 8,
    large = 15,
    XL = 25 // Adding for later checks
}